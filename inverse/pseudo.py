import numpy as np

from cholesky import Cholesky
from sum import KahanSum


def pinv(A):
    """
    Computes the Moore-Penrose inverse of an mxn
    matrix A.

    To be completed using SVD when SVD is done.

    Params
    ------
    - A: a numpy array of shape (M, N).

    Returns
    -------
    - a numpy array of shape (N, M).
    """
    pass
